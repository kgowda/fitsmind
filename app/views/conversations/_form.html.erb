<%= form_with(model: conversation, local: true) do |form| %>
  <% if conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
      <% conversation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user1 %>
    <%= form.number_field :user1, id: :conversation_user1 %>
  </div>

  <div class="field">
    <%= form.label :user2 %>
    <%= form.number_field :user2, id: :conversation_user2 %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :conversation_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
