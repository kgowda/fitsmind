<%= form_with(model: chat, local: true) do |form| %>
  <% if chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat.errors.count, "error") %> prohibited this chat from being saved:</h2>

      <ul>
      <% chat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :chat_message %>
  </div>

  <div class="field">
    <%= form.label :message_id %>
    <%= form.number_field :message_id, id: :chat_message_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
